<div class="problem-anchor" id="problem-lc-102"></div>

??? problem "LeetCode 102 · 二叉树的层序遍历"
    [打开原题 ↗](https://leetcode.cn/problems/binary-tree-level-order-traversal/){ .problem-source }

    **难度与分值**：LeetCode 官方「中等」。

    **题意**：按从上到下、每层从左到右的顺序返回二叉树节点值。

    **思路**：队列开始处理一层时先固定当前长度，只弹出这些节点并把孩子加入下一层，从而让队列边界直接表示深度边界。

    **复杂度**：时间 $O(n)$，队列空间 $O(w)$，其中 $w$ 为最大层宽。

    **C++ 实现**

    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    struct TreeNode {
      int val;
      TreeNode* left;
      TreeNode* right;
    };
    class Solution {
    public:
      vector<vector<int>> levelOrder(TreeNode* root) {
        vector<vector<int>> answer;
        if (!root) {
          return answer;
        }
        queue<TreeNode*> queue;
        queue.push(root);
        while (!queue.empty()) {
          int layerSize = queue.size();
          vector<int> layer;
          layer.reserve(layerSize);
          while (layerSize--) {
            TreeNode* node = queue.front();
            queue.pop();
            layer.push_back(node->val);
            if (node->left) {
              queue.push(node->left);
            }
            if (node->right) {
              queue.push(node->right);
            }
          }
          answer.push_back(std::move(layer));
        }
        return answer;
      }
    };
    ```
