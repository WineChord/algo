<div class="problem-anchor" id="problem-lc-32"></div>

??? problem "LeetCode 32 · 最长有效括号"
    [打开原题 ↗](https://leetcode.cn/problems/longest-valid-parentheses/){ .problem-source }

    **难度与分值**：LeetCode 官方「困难」。

    **题意**：求只含圆括号的字符串中最长连续合法括号子串的长度。

    **思路**：栈底保存最近一个无法匹配的右括号位置，其上保存尚未匹配的左括号位置。每次成功配对后，当前位置与新栈顶的距离就是当前合法后缀长度。

    **复杂度**：时间 $O(n)$，额外空间 $O(n)$。

    **C++ 实现**

    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    class Solution {
    public:
      int longestValidParentheses(string s) {
        vector<int> stack = {-1};
        int answer = 0;
        for (int i = 0; i < static_cast<int>(s.size()); ++i) {
          if (s[i] == '(') {
            stack.push_back(i);
          } else {
            stack.pop_back();
            if (stack.empty()) {
              stack.push_back(i);
            } else {
              answer = max(answer, i - stack.back());
            }
          }
        }
        return answer;
      }
    };
    ```
