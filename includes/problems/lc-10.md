<div class="problem-anchor" id="problem-lc-10"></div>

??? problem "LeetCode 10 · 正则表达式匹配"
    [打开原题 ↗](https://leetcode.cn/problems/regular-expression-matching/){ .problem-source }

    **难度与分值**：LeetCode 官方「困难」。

    **题意**：判断由普通字符、`.` 与 `*` 构成的模式是否完整匹配整个字符串。

    **思路**：令 `dp[i][j]` 表示两侧前缀完整匹配；普通字符从左上转移，`*` 分成跳过前一原子或消费一个匹配字符。

    **复杂度**：时间 $O(nm)$，空间 $O(nm)$；只求布尔值可滚动到 $O(m)$。

    **C++ 实现**

    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    class Solution {
    public:
      bool isMatch(string s, string p) {
        int n = s.size();
        int m = p.size();
        vector<vector<char>> dp(n + 1, vector<char>(m + 1));
        dp[0][0] = true;
        for (int j = 2; j <= m; ++j) {
          if (p[j - 1] == '*')
            dp[0][j] = dp[0][j - 2];
        }
        for (int i = 1; i <= n; ++i) {
          for (int j = 1; j <= m; ++j) {
            if (p[j - 1] == '*') {
              dp[i][j] = dp[i][j - 2];
              bool matches = p[j - 2] == '.' || p[j - 2] == s[i - 1];
              dp[i][j] |= matches && dp[i - 1][j];
            } else {
              bool matches = p[j - 1] == '.' || p[j - 1] == s[i - 1];
              dp[i][j] = matches && dp[i - 1][j - 1];
            }
          }
        }
        return dp[n][m];
      }
    };
    ```
