<div class="problem-anchor" id="problem-lc-207"></div>

??? problem "LeetCode 207 · 课程表"
    [打开原题 ↗](https://leetcode.cn/problems/course-schedule/){ .problem-source }

    **难度与分值**：LeetCode 官方「中等」。

    **题意**：给定课程先修关系，判断能否完成全部课程。

    **思路**：把先修关系建成有向图，反复取入度为零的课程并删除其出边。若最终处理了全部节点则存在拓扑序，否则剩余节点处在有向环中。

    **复杂度**：时间 $O(n+m)$，额外空间 $O(n+m)$。

    **C++ 实现**

    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    class Solution {
    public:
      bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        vector<vector<int>> graph(numCourses);
        vector<int> indegree(numCourses);
        for (const auto& edge : prerequisites) {
          graph[edge[1]].push_back(edge[0]);
          ++indegree[edge[0]];
        }
        queue<int> pending;
        for (int course = 0; course < numCourses; ++course) {
          if (indegree[course] == 0) {
            pending.push(course);
          }
        }
        int completed = 0;
        while (!pending.empty()) {
          int course = pending.front();
          pending.pop();
          ++completed;
          for (int next : graph[course]) {
            if (--indegree[next] == 0) {
              pending.push(next);
            }
          }
        }
        return completed == numCourses;
      }
    };
    ```
