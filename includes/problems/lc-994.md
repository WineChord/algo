<div class="problem-anchor" id="problem-lc-994"></div>

??? problem "LeetCode 994 · 腐烂的橘子"
    [打开原题 ↗](https://leetcode.cn/problems/rotting-oranges/){ .problem-source }

    **难度与分值**：LeetCode 官方「中等」。

    **题意**：腐烂橘子每分钟感染四邻新鲜橘子，求全部腐烂的最短时间；无解返回 $-1$。

    **思路**：把所有初始腐烂格同时作为第 0 层源点做 BFS。队列层数就是最早感染时间，剩余新鲜橘子用于判断无解。

    **复杂度**：时间 $O(mn)$，队列空间 $O(mn)$。

    **C++ 实现**

    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    class Solution {
    public:
      int orangesRotting(vector<vector<int>>& grid) {
        int rows = grid.size();
        int columns = grid[0].size();
        queue<pair<int, int>> queue;
        int fresh = 0;
        for (int row = 0; row < rows; ++row) {
          for (int column = 0; column < columns; ++column) {
            if (grid[row][column] == 2) {
              queue.push({row, column});
            } else if (grid[row][column] == 1) {
              ++fresh;
            }
          }
        }
        const int direction[5] = {-1, 0, 1, 0, -1};
        int minutes = 0;
        while (fresh > 0 && !queue.empty()) {
          int layerSize = queue.size();
          while (layerSize--) {
            auto [row, column] = queue.front();
            queue.pop();
            for (int d = 0; d < 4; ++d) {
              int nextRow = row + direction[d];
              int nextColumn = column + direction[d + 1];
              if (nextRow < 0 || nextRow >= rows || nextColumn < 0 || nextColumn >= columns ||
                  grid[nextRow][nextColumn] != 1) {
                continue;
              }
              grid[nextRow][nextColumn] = 2;
              --fresh;
              queue.push({nextRow, nextColumn});
            }
          }
          ++minutes;
        }
        return fresh == 0 ? minutes : -1;
      }
    };
    ```
