<div class="problem-anchor" id="problem-lc-347"></div>

??? problem "LeetCode 347 · 前 K 个高频元素"
    [打开原题 ↗](https://leetcode.cn/problems/top-k-frequent-elements/){ .problem-source }

    **难度与分值**：LeetCode 官方「中等」。

    **题意**：返回数组中出现频率最高的 $k$ 个不同元素，答案集合保证唯一。

    **思路**：哈希统计后按频率放入 $0\ldots n$ 的桶，从高频向低频收集；频率值域被数组长度天然界定。

    **复杂度**：时间 $O(n)$，额外空间 $O(n)$。

    **C++ 实现**

    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    class Solution {
    public:
      vector<int> topKFrequent(vector<int>& nums, int k) {
        unordered_map<int, int> frequency;
        frequency.reserve(nums.size() * 2);
        for (int value : nums) {
          ++frequency[value];
        }
        vector<vector<int>> bucket(nums.size() + 1);
        for (auto [value, count] : frequency) {
          bucket[count].push_back(value);
        }
        vector<int> answer;
        answer.reserve(k);
        for (int count = nums.size(); count >= 1 && static_cast<int>(answer.size()) < k; --count) {
          for (int value : bucket[count]) {
            answer.push_back(value);
            if (static_cast<int>(answer.size()) == k) {
              break;
            }
          }
        }
        return answer;
      }
    };
    ```
