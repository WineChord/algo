<div class="problem-anchor" id="problem-lc-162"></div>

??? problem "LeetCode 162 · 寻找峰值"
    [打开原题 ↗](https://leetcode.cn/problems/find-peak-element/){ .problem-source }

    **难度与分值**：LeetCode 官方「中等」。

    **题意**：返回数组中任意一个严格大于左右相邻元素的峰值下标，边界外视为负无穷，并满足 O(log n) 时间要求。

    **思路**：比较中点与右邻：上坡时右半必含峰值，否则左半（含中点）必含峰值。每轮保留至少一个峰值所在的闭区间，直到只剩一个位置。

    **复杂度**：时间 O(log n)，空间 O(1)。

    **C++ 实现**

    <!-- compile:standalone -->
    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    class Solution {
    public:
      int findPeakElement(vector<int>& nums) {
        int left = 0;
        int right = static_cast<int>(nums.size()) - 1;
        while (left < right) {
          int middle = left + (right - left) / 2;
          if (nums[middle] < nums[middle + 1]) left = middle + 1;
          else right = middle;
        }
        return left;
      }
    };
    int main() {
      vector<int> nums{1, 2, 1, 3, 5, 6, 4};
      cout << Solution().findPeakElement(nums) << '\n';
    }
    ```
